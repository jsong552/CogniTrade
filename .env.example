# CogniTrade Environment Variables
# Copy this file to .env and fill in the values.

# --- OpenAI (required for the LangGraph AI agent) ---
OPENAI_API_KEY=your_openai_api_key_here
# Optional: BACKBOARD_API_KEY is also accepted as fallback for OPENAI_API_KEY.

# Optional: model used by the agent (default: gpt-4o).
# BACKBOARD_MODEL_NAME=gpt-4o
# OPENAI_MODEL_NAME=gpt-4o

# --- Gradium Speech-to-Text (required for voice journaling) ---
GRADIUM_API_KEY=your_gradium_api_key_here
# GRADIUM_REGION=us

# --- Alpaca Trading API (used by frontend) ---
# VITE_ALPACA_API_KEY=your_alpaca_key
# VITE_ALPACA_SECRET_KEY=your_alpaca_secret

# --- Finnhub (used by frontend for market data) ---
# VITE_FINNHUB_API_KEY=your_finnhub_key
